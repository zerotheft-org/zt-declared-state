apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zerotheft-prod-tls
  namespace: zerotheft-prod
spec:
  secretName: zerotheft-prod-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - api.zerotheft.com
    - app.zerotheft.com
