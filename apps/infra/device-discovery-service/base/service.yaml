apiVersion: v1
kind: Service
metadata:
  name: device-discovery-service
  labels:
    app: device-discovery-service
spec:
  selector:
    app: device-discovery-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
