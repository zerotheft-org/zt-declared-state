apiVersion: v1
kind: Service
metadata:
  name: flutter-app
  labels:
    app: flutter-app
spec:
  selector:
    app: flutter-app
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
